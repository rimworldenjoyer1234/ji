# external/jitterentropy

Placeholder directory for integrating Stephan Mueller's jitterentropy-library.

The runtime wrapper currently compiles without this dependency by using an internal fallback backend adapter.
See TODO markers in `src/entropy_backend.c` for exact integration points.
